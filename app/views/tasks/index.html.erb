<ul>
<% @tasks.each do |task| %>
<li><%= link_to task.title, task_path(task) %> <%= link_to "🚽", task_path(task), data: {turbo_method: :delete, turbo_confirm: "Did the boys give you permission?"} %> </li>
<% end %>
</ul>


<%= link_to "Add a new task", tasks_new_path %>
